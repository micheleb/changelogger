# Port for the changelogger API server
PORT=3000

# Comma-separated list of repository folder names to serve
# These folders should be in the same directory as the changelogger server
# Each folder should contain a CHANGELOG.md file in Keep a Changelog format
REPOS=my-project,another-project,third-project

# Optional: Base path for repositories (defaults to current directory)
# REPOS_BASE_PATH=/path/to/repos

# SQLite caching configuration
# Enable/disable SQLite caching for parsed changelog data (default: false)
SQLITE_CACHE=false

# Path to SQLite cache database file (default: ./cache.db)
# SQLITE_CACHE_DB_PATH=./cache.db

# Cache TTL in hours - entries older than this are cleaned up (default: 168 = 7 days)
# SQLITE_CACHE_TTL_HOURS=168

# Include release dates in markdown output (default: false)
# When enabled, each changelog entry will show the release date in format "Apr 25, 2025"
# Example: "- [2.1.0] (Apr 25, 2025) Real-time notifications"
WITH_DATES=false